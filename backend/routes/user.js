//as we did for the workouts,we need to create routes for users

const express=require("express");

const router=express.Router();

//login route
router.post('/login',()=>{});



//sign up route
router.post('/signup',()=>{})

module.exports=router
